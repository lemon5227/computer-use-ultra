# Create this key from Vercel AI Gateway, then export it in your shell.
# Computer Use Ultra never ships or prints this value.
AI_GATEWAY_API_KEY=

# Optional when running inside a Vercel deployment.
VERCEL_OIDC_TOKEN=
